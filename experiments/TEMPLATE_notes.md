# <ID> — <short title>

> Commit the **Question / Hypothesis / Baseline / Variable** sections BEFORE you run anything: commit timestamps are
> the proof of when the hypothesis was written. Then fill in the rest.

## 1. Question
One sentence.

## 2. Hypothesis and prediction (written before running)
"I expect X because Y; if I'm wrong, I'll see Z."

## 3. Baseline
Which config / run do you compare against?

## 4. One variable changed
What changes (config key and values). Everything else stays fixed:

## 5. Repetitions
Seeds used (≥ 3 for accuracy claims at Level 2), hardware, exact command(s):

## 6. Result
Table or plot (raw numbers in a file next to this one). Mean ± std over seeds.

## 7. Interpretation
What the result supports, what it does NOT support, other explanations. Dead ends and failed runs go here too.
